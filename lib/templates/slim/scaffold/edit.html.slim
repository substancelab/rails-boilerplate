header.header
  h1 Editing <%= singular_table_name %>

  = link_to "←", <%= index_helper %>_path, :class => "btn btn-secondary"

== render "form", :<%= singular_table_name %> => @<%= singular_table_name %>
